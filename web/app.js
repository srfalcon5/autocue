function handleMarquee() {
	const marquee = document.getElementById("marquee");
	let speed = 4 | customSpeed;
	let lastScrollPos = 0;
	let timer;

	const container = marquee.querySelector(".inner");
	const content = marquee.querySelector(".inner > p");
}

